tasks:
  -
    name: run-docker
    language: Bash
    default: true
    code: |
      sudo apk add docker-cli
      sudo docker run hello-world
      export cwd=$(pwd)
      sudo -E docker build $cwd/source
